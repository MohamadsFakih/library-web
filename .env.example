# Copy to .env and fill in values.

# -----------------------------------------------------------------------------
# Database (required)
# -----------------------------------------------------------------------------
# Local dev: SQLite (no Docker needed)
DATABASE_URL="file:./dev.db"

# Production (Vercel etc.): set DATABASE_URL in host dashboard to a Postgres URL.
# Build auto-switches to Postgres when it sees postgresql:// in DATABASE_URL.

# -----------------------------------------------------------------------------
# Auth (required for login/sessions)
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-at-least-32-chars

# -----------------------------------------------------------------------------
# Admin user (required for db:seed – creates/updates admin account)
# -----------------------------------------------------------------------------
ADMIN_PASSWORD=your-secure-admin-password
# ADMIN_EMAIL=admin@admin.com

# Production: set to your app URL (no trailing slash).
# NEXTAUTH_URL=https://your-app.vercel.app

# -----------------------------------------------------------------------------
# SSO (optional – Google & GitHub)
# -----------------------------------------------------------------------------
# Google: https://console.cloud.google.com/apis/credentials
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=

# GitHub: https://github.com/settings/developers
# AUTH_GITHUB_ID=
# AUTH_GITHUB_SECRET=

# -----------------------------------------------------------------------------
# Vercel Blob (optional – for book cover uploads to persist in production)
# -----------------------------------------------------------------------------
# On Vercel: Storage → Blob → connect to project. BLOB_READ_WRITE_TOKEN is set automatically.
# BLOB_READ_WRITE_TOKEN=

# -----------------------------------------------------------------------------
# OpenAI (optional – for AI book summarization)
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=sk-...
